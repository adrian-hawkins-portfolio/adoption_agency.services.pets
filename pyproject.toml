[project]
name = "services-pets"
version = "0.1.0"
description = ""
authors = [
    {name = "Your Name",email = "you@example.com"}
]
requires-python = ">=3.14"
dependencies = [
    "petstore-libs (>=0.1.0,<0.2.0)"
]

[[tool.poetry.source]]
name = "local"
url = "http://localhost/simple"
priority = "primary"

[tool.poetry]
packages = [{include = "pet_service", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
